# VCS + local metadata
.git
.gitignore
.DS_Store
**/.DS_Store

# Rust build output and local runtime state
target
**/target
.tmp
.rustyfin.runtime.env

# Local package/test caches
.npm-cache
.playwright-browsers
**/coverage
**/dist
**/.next
**/node_modules

# Local secrets and env files (never needed in build context)
.env
.env.*
!.env.example
!.env.template

# Documentation and reference bundles not required for image builds
docs
tests
watchparty_impliment

# UI sources are built from ./ui context, not from root context
ui
